version: '2.1'
orbs:
  typescript: vidsy/typescript@1.2.1
jobs:
  test:
    docker: 
      - image: 'cimg/base:stable'
    steps:
      - checkout
      - node/install:
          node-version: '18.3.0'
      - run: npm run test
workflows:
  test_my_app:
    jobs:
      - test
